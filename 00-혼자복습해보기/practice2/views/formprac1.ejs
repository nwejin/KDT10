<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>POST FORM 실습</h1>
    <form action="/1/postForm1" method="post">
      <label for="name">이름</label>
      <input type="text" name="name" id="" required />

      <fieldset>
        <legend>성별</legend>

        <input type="radio" name="gender" id="" value="남자" required />
        <label for="">남자</label>

        <input type="radio" name="gender" id="" value="여자" />
        <label for="">여자</label>
      </fieldset>

      <fieldset>
        <legend>생년월일</legend>
        <select name="year" id="">
          <% for (let i = 0; i<30; i ++) {%>
          <option value="<%= 2023-i%>"><%=2023-i%></option>
          <% }%>
        </select>
        <label for="year">년</label>
        <select name="month" id="">
          <% for (let i = 1; i<=12; i ++) {%>
          <option value="<%= i%>"><%=i%></option>
          <% }%>
        </select>
        <label for="month">월</label>
        <select name="day" id="">
          <% for (let i = 1; i<=31; i ++) {%>
          <option value="<%= i%>"><%=i%></option>
          <% }%>
        </select>
        <label for="day">일</label>
      </fieldset>

      <fieldset>
        <legend>관심사</legend>
        <input type="checkbox" name="value" id="" value="여행" />
        <label for="value">여행</label>

        <input type="checkbox" name="value" id="" value="패션" />
        <label for="value">패션</label>

        <input type="checkbox" name="value" id="" value="음식" />
        <label for="value">음식</label>
      </fieldset>

      <button type="button" onclick="axiosPost()">회원가입</button>

      <script>
        function axiosPost() {
          axios({
            method: "POST",
            url: "axiosPost",
            data: data,
          }).then((response) => {
            console.log(response);
            console.log(response.data);
          });
        }
      </script>
    </form>
  </body>
</html>
